services:
  - type: web
    name: ai-emergency-plan-worker
    runtime: python
    rootDir: apps/worker
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    plan: free
